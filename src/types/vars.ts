 // Valores por defecto para rellenar un criterio si falta
export const defaultCriterion = {
  nota: 0,
  razon: "No pudimos evaluar este criterio.",
};

// Valores por defecto para rellenar el resultado final si falta
export const defaultResult = {
  nota: 0,
  razon: "No pudimos evaluar este criterio."
};